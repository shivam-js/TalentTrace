{
  "name": "talenttrace",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-based technical hiring workspace for human interviewers.",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "build": "pnpm --recursive run build",
    "check": "pnpm --recursive run check",
    "test": "pnpm --recursive run test"
  }
}
